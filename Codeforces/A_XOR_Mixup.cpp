#include<bits/stdc++.h>

using namespace std;



int main() {
    int t; // Number of test cases
    cin >> t;

    while (t--) {
        int n; // Number of integers in array a
        cin >> n;

        vector<int> a(n);

        // Input elements of array a
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }

        

        
    }

    return 0;
}